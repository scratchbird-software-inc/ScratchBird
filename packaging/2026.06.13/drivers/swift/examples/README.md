# ScratchBird swift driver example

The canonical executable example for this package is `bin/sb_isql_swift`. It accepts the shared sb_isql conformance command-line contract and shows connection setup, input/output/error routing, diagnostics, metrics, and server-parser execution for this driver.
