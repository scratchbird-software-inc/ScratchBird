# Reference Parsers

Compatibility/reference parser packages belong here after they pass their
public parser proof gates. Original engine regression suites and original
engine tools used for tests remain under `project/tests`, not in this bundle,
unless they are intentionally packaged as test-run downloads with provenance.
