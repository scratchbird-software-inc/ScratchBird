[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scratchbird-flightsql"
version = "0.0.0"
description = "ScratchBird Flight SQL host-route driver lane"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
