# Examples

Runnable examples for local development and integration validation.

- `http-bridge.env.example` is the base environment template for live bridge
  bring-up.
- The file is POSIX-shell friendly.
- Windows users should copy the same variable values into PowerShell
  environment variables instead of sourcing the file directly.
