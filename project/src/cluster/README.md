# Cluster

Private cluster authority: membership, decision service, epochs, fences, routes, placement, reconciliation, distributed transactions, and replication.

This tree is private/trade-secret and must not be included in public/open-source packages.
