# SBSQL source-layout registry fragment: agents
# This fragment is the future source-of-editing for agents registry rows.
# Existing aggregate registries remain authoritative until a later registry
# migration execution_plan moves rows into fragments.
family: agents
rows: []
