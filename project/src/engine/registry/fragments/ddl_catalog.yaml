# SBSQL source-layout registry fragment: ddl_catalog
# This fragment is the future source-of-editing for ddl_catalog registry rows.
# Existing aggregate registries remain authoritative until a later registry
# migration execution_plan moves rows into fragments.
family: ddl_catalog
rows: []
