# Driver Source Provenance

Search key: `DRIVER_SOURCE_PROVENANCE`

The imported source baseline is the current `project/drivers/` tree described
by `project/drivers/IMPORT_MANIFEST.md`.

Current destination roots:

- `project/drivers/driver/`
- `project/drivers/adaptor/`
- `project/drivers/tool/`

The old external `ScratchBird-driver` repository is no longer a live build,
test, or benchmark dependency for this execution_plan. Historical references may
mention it only as preserved evidence; current build and test logic must not.
