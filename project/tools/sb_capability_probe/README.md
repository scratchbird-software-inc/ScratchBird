# sb_capability_probe

Private runtime capability probe for mandatory int128, uint128, real128, and LLVM capability reporting. GPU remains optional and is reported as a capability, not a mandatory dependency.
