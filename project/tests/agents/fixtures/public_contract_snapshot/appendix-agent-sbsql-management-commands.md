# Appendix: Agent SBSQL Management Commands

SEARCH_KEY: AGENT_SBSQL_MANAGEMENT_COMMANDS_FIXTURE

This fixture snapshot defines the checked-in command surface used by agent runtime gates.
Each command row must map to an SBLR/API operation, authorization rule, evidence row,
result shape, and diagnostic code before it can be exposed.

