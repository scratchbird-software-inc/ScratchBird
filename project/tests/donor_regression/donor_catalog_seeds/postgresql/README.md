# PostgreSQL donor catalog seed manifests

Search key: `SB_REFERENCE_DONOR_CATALOG_SEED_POSTGRESQL`

This directory is reserved for exact newly-created PostgreSQL donor catalog seed manifests.

Each supported donor version/profile must provide a manifest conforming to `SB_SPEC_DONOR_CATALOG_SEED_MANIFEST` before PostgreSQL emulation setup can be considered implementation-ready.
