{
  "artifact_role": "readiness_control_fixture_manifest",
  "authority_boundary": "Fixture evidence is conformance input only; it is not product behavior authority.",
  "canonical_sources": [
    "fixture://execution-plans/driver-parser-sblr-sbsql-readiness-closure/SERVER_REVALIDATION_CONTRACT_MATRIX.csv#contract_id",
    "public_contract_snapshot"
  ],
  "cases": [
    {
      "case_id": "DPR-FIX-008-POS-001",
      "case_kind": "positive_control",
      "expected_gate_behavior": "Pass fixture catalog materialization checks only.",
      "purpose": "Verify the indexed canonical sources and consumer handoff are present before implementation agents start."
    },
    {
      "case_id": "DPR-FIX-008-NEG-001",
      "case_kind": "negative_control",
      "expected_gate_behavior": "Owning semantic gate must block or emit exact refusal until canonical implementation evidence exists.",
      "purpose": "stale UUID"
    },
    {
      "case_id": "DPR-FIX-008-NEG-002",
      "case_kind": "negative_control",
      "expected_gate_behavior": "Owning semantic gate must block or emit exact refusal until canonical implementation evidence exists.",
      "purpose": "grant mismatch"
    },
    {
      "case_id": "DPR-FIX-008-NEG-003",
      "case_kind": "negative_control",
      "expected_gate_behavior": "Owning semantic gate must block or emit exact refusal until canonical implementation evidence exists.",
      "purpose": "descriptor epoch mismatch"
    },
    {
      "case_id": "DPR-FIX-008-NEG-004",
      "case_kind": "negative_control",
      "expected_gate_behavior": "Owning semantic gate must block or emit exact refusal until canonical implementation evidence exists.",
      "purpose": "wrong language resource"
    }
  ],
  "consumer_agents": [
    "server_revalidation_agent",
    "driver_lane_agents"
  ],
  "covered_gates": [
    "DPR-GATE-009B"
  ],
  "fixture_family": "server_revalidation_fixtures",
  "fixture_id": "DPR-FIX-008",
  "forbidden_fixture_source": "driver accepted payload output"
}
