CEIC execution protocol
=======================

Run `ctest --test-dir build --output-on-failure -R '^ceic_09'` after changing
integrated CEIC evidence. Run the public reference and secret scans after any
artifact path change.
