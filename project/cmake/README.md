# CMake Support

CMake modules, compiler profiles, feature checks, generated-artifact hooks, and packaging helpers live here.

No subsystem behavior is specified by build scripts. Build scripts consume the contracts and implementation targets.
